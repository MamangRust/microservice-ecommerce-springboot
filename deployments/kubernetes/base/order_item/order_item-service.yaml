apiVersion: v1
kind: Service
metadata:
  name: order-item
  namespace: ecommerce
spec:
  selector:
    app: order-item
  ports:
    - name: http
      port: 8086
      targetPort: 8086
    - name: grpc
      port: 9019
      targetPort: 9019
  type: ClusterIP
