apiVersion: v1
kind: Service
metadata:
  name: merchant-policy
  namespace: ecommerce
spec:
  selector:
    app: merchant-policy
  ports:
    - name: http
      port: 8088
      targetPort: 8088
    - name: grpc
      port: 9023
      targetPort: 9023
  type: ClusterIP
